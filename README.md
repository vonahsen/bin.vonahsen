# bin.vonahsen

silly scripts and executables for various sysadmin tasks

scripts:
* dns_stats.sh: analyzer for pihole dnsmasq log files.  intended to run via cron, it sends an email with the number of queries, query types, top lookup targets, and top clients


